export default function Page() {
  return (
    <div>
      R4: Catch all, you cannot use both an required and optional catch-all route at the same level, catch all dengan satu
      kurung siku, optional catcha-all dengan dua kurung siku di dalam kurung siku
    </div>
  );
}
